# ЭТАП 2: Ядро игровой логики — Контекст для запуска

**Статус:** ЭТАП 1 завершён ✅ (коммит d054bfe)

**Текущая ветка:** `claude/create-dev-plan-HIvE6`

## 🎯 Цель ЭТАПА 2

Реализовать ядро игровой логики: механику дневного цикла, экономические расчёты, систему складов и события.

## 📋 Подзадачи ЭТАПА 2

### 2.1 Определить типы и интерфейсы
**Файл:** `src/types/game.ts` (уже частично создан, расширить)
- ✓ GameState
- ✓ Event, EventOption
- ✓ DayResult
- ✓ Service
- ✓ BusinessConfig
- ❌ Расширить: Modifiers, SeasonalityData, StockBatch

### 2.2 Реализовать `processDay()` функцию
**Файл:** `src/services/dayCalculator.ts` (новый)
```typescript
// Основная функция дневного цикла
function processDay(gameState: GameState): DayResult

// Проверки блокировок
function checkDayBlocked(state: GameState): { blocked: boolean, reason?: string }

// Запуск дня: расчёт дохода, расходов, проверки
```

### 2.3 Экономический движок
**Файл:** `src/services/economyEngine.ts` (новый)
```typescript
// Расчёт клиентов с модификаторами (сезон, реклама, репутация)
function calculateClients(base: number, modifiers: Modifiers): number

// Расчёт дохода
function calculateRevenue(served: number, avgCheck: number, modifiers: Modifiers): number

// Расчёт расходов (закупки, налоги, подписки, аренда)
function calculateExpenses(state: GameState): { daily: number, monthly: number }

// Применение эффектов сервисов
function applyServiceEffects(state: GameState, modifiers: Modifiers): void
```

### 2.4 Система склада (FIFO, просрочка)
**Файл:** `src/services/stockManager.ts` (новый)
```typescript
// Добавление товара на склад
function addStock(state: GameState, quantity: number, cost: number): void

// Списание товара (FIFO)
function consumeStock(state: GameState, quantity: number): { cost: number, losses: number }

// Проверка просроченного товара
function checkExpiry(state: GameState): { expired: number, loss: number }

// Процент заполнения склада
function getStockPercentage(state: GameState): number
```

### 2.5 Генерация и обработка событий
**Файл:** `src/services/eventGenerator.ts` (новый)
```typescript
// Генерация случайного события
function generateEvent(day: number, state: GameState): Event | null

// Обработка выбранного варианта события
function applyEventConsequence(state: GameState, event: Event, optionId: string): void

// Эвент-лист (15-20 событий)
const EVENTS_DATABASE: EventTemplate[]
```

### 2.6 Проверка условий победы и поражения
**Файл:** `src/services/victoryChecker.ts` (новый)
```typescript
// Проверка банкротства
function checkBankruptcy(state: GameState): boolean

// Проверка условий победы (5 условий)
function checkVictory(state: GameState): boolean

// Получить статус всех условий победы
function getVictoryStatus(state: GameState): { [key: string]: boolean }
```

## 📦 Конфигурационные константы

**Файл:** `src/constants/business.ts` (новый)
```typescript
// Параметры трёх типов бизнеса
export const BUSINESS_CONFIGS: Record<BusinessType, BusinessConfig>

// Параметры сервисов (цены, эффекты, синергии)
export const SERVICES_CONFIG: Record<ServiceType, ServiceConfig>

// Констант экономики (налоги, базовые цены, модификаторы)
export const ECONOMY_CONSTANTS: {
  TAX_RATE: 0.06
  MONTHLY_EXPENSES: Record<BusinessType, number>
  EXPIRY_RATES: Record<BusinessType, number>
  // и т.д.
}
```

## ✅ Определение "готово"

ЭТАП 2 считается завершённым, когда:
- [ ] 2.1: Все типы определены и типизированы
- [ ] 2.2: `processDay()` работает, проверяет блокировки
- [ ] 2.3: Расчёты дохода и расходов корректны
- [ ] 2.4: Система склада работает с FIFO и просрочкой
- [ ] 2.5: События генерируются и применяются
- [ ] 2.6: Проверки победы/поражения работают
- [ ] Юнит-тесты для всех функций (100% coverage)
- [ ] Все компиляется без ошибок (npm run type-check)
- [ ] Коммит с полным описанием

## 🧪 Тестирование ЭТАПА 2

```bash
# Запуск тестов
npm run test -- --coverage

# Проверка типов
npm run type-check

# Запуск dev сервера для ручного тестирования
npm run dev
```

## 🔗 Связанные документы

- `GDD Executive Summary.md` — общее описание игры
- `Техническое задание на ядро игрового цикла.md` — псевдокод `processDay()`
- `Экономическая спецификация.md` — формулы и коэффициенты
- `CLAUDE.md` — полный план разработки

---

**Готово к началу? Скопируй контекст и начни ЭТАП 2! 🚀**
